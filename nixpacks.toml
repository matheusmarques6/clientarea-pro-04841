# Nixpacks Configuration
# This ensures Railway uses ONLY Bun, not Deno

[providers]
# Force Bun as the only provider
bun = "1.3.8"

[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run preview"
